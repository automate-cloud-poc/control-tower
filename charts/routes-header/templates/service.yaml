{{- $name := .Values.name -}}
{{- $svcPort := .Values.service.port -}}

{{- range $canaryHeader := .Values.canaryHeader }}
---
apiVersion: v1
kind: Service
metadata:
  name: "{{ $name }}-{{$canaryHeader.commit}}"
  namespace: api
  labels:
    - "{{ $name }}-{{$canaryHeader.commit}}"
spec:
  type: ClusterIP
  ports:
    - port: {{ $svcPort}}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: "{{ $name }}-{{$canaryHeader.commit}}"
    app.kubernetes.io/instance: "{{ $name }}-{{$canaryHeader.commit}}"
{{- end }}